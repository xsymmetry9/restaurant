{
  "name": "restaurant",
  "version": "1.0.0",
  "repository": {
    "url": "git+https://github.com/xsymmetry9/restaurant.git"
  },
  "description": "My dumpling shop",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "webpack --watch",
    "build": "webpack --mode=production",
    "dev": "webpack serve",
    "predeploy": "npm run build",
    "gh-deploy": "git push origin :gh-pages && git subtree push --prefix dist origin gh-pages",
    "gh-deploy-init": "git push origin && git subtree push --prefix dis origin gh-pages"
  },
  "author": "Gary Lei",
  "license": "ISC",
  "devDependencies": {
    "css-loader": "^6.7.3",
    "gh-pages": "^5.0.0",
    "html-webpack-plugin": "^5.5.1",
    "style-loader": "^3.3.2",
    "webpack": "^5.79.0",
    "webpack-cli": "^5.0.1",
    "webpack-dev-server": "^4.13.3"
  }
}
